# Auto-File-Organizer
 Smart File Organizer is a Python automation tool that automatically sorts files into organized folders based on their file types. It supports multiple categories, prevents duplicate filenames, creates folders automatically, and includes a dry-run mode to preview changes before moving files.
